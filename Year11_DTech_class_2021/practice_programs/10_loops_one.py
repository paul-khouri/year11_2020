# loop using a counter condition
# will run for a specific number of times
counter = 1
while counter <= 1000:
    output = "The counter is {}".format(counter)
    print(output)
    # incrementing (adding one to counter)
    counter += 1
